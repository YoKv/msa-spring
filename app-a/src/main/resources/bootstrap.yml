spring:
  application:
    name: app-a
  cloud:
    consul:
      host: dev.aiyo.space
      port: 8500
server:
  port: 0
feign:
  okhttp:
    enable: true
  httpclient:
    enabled: false
  hystrix:
    enabled: true
logging:
  level:
    yokv.service: debug